# ContextFlow

Privacy-preserving reimbursement agent built for the AWS Agents for Humans / Strands Agents hackathon.

> The model gets the context it needs to reason, but never the authority it needs to leak a secret.

This repository is being built around one airtight workflow: evidence-backed conference reimbursement, deterministic policy enforcement, constrained secret disclosure, browser execution, and independent proof of completion.
